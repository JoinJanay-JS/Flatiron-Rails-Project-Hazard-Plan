<div class="flex flex-wrap items-center mb-5 text-indigo-darker">
  <div class="flex-1 flex justify-between items-center">
    <div><h3 class="text-lg"><%= user.name %></h3> <%= user.username %> <% if current_user %>(You)<% end %></div>
    <p>Joined on <%= readable_time(user.created_at) %></p>

  </div>
  <%= link_to 'Delete User', users_path, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-destroy ml-4" if admin? %>
</div>

<% if user.schedules.any? %>
  <div class="block pl-2 border-l border-indigo-light mb-5">
    <p class="text-xs uppercase tracking-loose text-grey-darker pt-1">Upcoming Schedules</p>
    <% user.schedules.each do |schedule| %>
        <%= link_to schedule.name, schedule, class: "link my-2 inline-block" %>
    <% end %>
  </div>
<% end %>